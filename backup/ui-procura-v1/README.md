# UI Backup: procura v1

This folder contains a snapshot of key UI files before the next set of layout/navigation tweaks.

Restore by copying the files back to their original locations under `src/`.

